import { IThemeClasses } from "@app/types/theme";

export const iconDefaultClasses: IThemeClasses = {
  transition: "transition-all",
  transitionTiming: "ease-in-out",
  transitionDuration: "duration-default"
};
